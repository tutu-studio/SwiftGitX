enum GitDirectoryConstants {
    /// The directory of the heads (branches).
    static let heads = "refs/heads/"

    /// The directory of the remotes.
    static let remotes = "refs/remotes/"

    /// The directory of the tags.
    static let tags = "refs/tags/"
}
